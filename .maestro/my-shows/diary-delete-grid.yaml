# My Shows — Diary Delete (Grid View)
# Verifies: single-toggle delete pattern (tap to arm, tap to confirm)

appId: com.broadwayscorecard.app
---
- launchApp

# Navigate to My Shows tab and sign in with dev bypass
- tapOn: "My Shows"
- tapOn:
    id: "Dev Sign In"

- tapOn:
    id: "Diary"

# Note: Grid view + grid delete not yet implemented in mobile.
# Stub for when it is added.
# - tapOn:
#     id: "Grid view"
