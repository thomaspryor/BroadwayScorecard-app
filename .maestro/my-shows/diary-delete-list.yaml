# My Shows — Diary List View
# Verifies: diary tab loads with entries and stats

appId: com.broadwayscorecard.app
---
- launchApp
- openLink: broadwayscorecard:///my-shows

# Diary tab active
- assertVisible: "Diary"

# Sort button visible
- assertVisible:
    text: "Sort diary.*"
